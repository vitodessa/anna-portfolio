# Портфолио Анны Подолян

Одностраничный сайт-каталог работ по digital-маркетингу. Публикуется через GitHub Pages из ветки `main`.

Все проекты — массив `PROJECTS` в начале `<script>` в `index.html`; новый проект = одна запись.
